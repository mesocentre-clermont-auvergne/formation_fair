name: qc # conda environment name
channels: 
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - fastqc=0.11.9 # quality check of fastq data (java)
  - multiqc=1.14 # reports aggregation (R package)
